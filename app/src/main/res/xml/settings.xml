<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory
        android:title="Inline Preferences">

        <CheckBoxPreference
            android:key="checkbox_preference"
            android:title="Check Pref"
            android:summary="More information here" />

    </PreferenceCategory>

    <PreferenceCategory
        android:title="Dialog Preferences">

        <EditTextPreference
            android:key="edittext_preference"
            android:title="Name"
            android:summary="Set your name"
            android:dialogTitle="The Dialog title" />

        <!--<ListPreference-->
            <!--android:key="list_preference"-->
            <!--android:title="@string/title_list_preference"-->
            <!--android:summary="@string/summary_list_preference"-->
            <!--android:entries="@array/entries_list_preference"-->
            <!--android:entryValues="@array/entryvalues_list_preference"-->
            <!--android:dialogTitle="@string/dialog_title_list_preference" />-->

    </PreferenceCategory>

    <PreferenceCategory
        android:title="Launch Preferences">

        <!-- This PreferenceScreen tag serves as a screen break (similar to page break
             in word processing). Like for other preference types, we assign a key
             here so it is able to save and restore its instance state. -->
        <PreferenceScreen
            android:key="screen_preference"
            android:title="Screen Preferences"
            android:summary="Settings for the screen">

            <!-- You can place more preferences here that will be shown on the next screen. -->

            <CheckBoxPreference
                android:key="next_screen_checkbox_preference"
                android:title="Toggle Preference"
                android:summary="Summary text" />

        </PreferenceScreen>

        <PreferenceScreen
            android:title="Intent preferences"
            android:summary="summary">

            <intent android:action="android.intent.action.VIEW"
                android:data="http://www.android.com" />

        </PreferenceScreen>

    </PreferenceCategory>

    <PreferenceCategory
        android:title="Attrs">

        <CheckBoxPreference
            android:key="parent_checkbox_preference"
            android:title="Parent Preference"
            android:summary="Summary" />

        <!-- The visual style of a child is defined by this styled theme attribute. -->
        <CheckBoxPreference
            android:key="child_checkbox_preference"
            android:dependency="parent_checkbox_preference"
            android:layout="?android:attr/preferenceLayoutChild"
            android:title="Child"
            android:summary="I am a child" />

    </PreferenceCategory>

</PreferenceScreen>